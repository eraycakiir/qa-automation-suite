name: Run Tagged Playwright + API Tests

on:
  workflow_dispatch:
    inputs:
      group:
        description: 'Test group (smoke, regression, api-only, ui-only)'
        required: true
        default: 'smoke'

jobs:
  docker-allure:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      - name: 🔄 Reuse Allure history (if exists)
        run: |
          mkdir -p allure-results/history
          if [ -d allure-report/history ]; then
            cp -r allure-report/history/* allure-results/history/
          else
            echo "No previous history found, skipping."
          fi

      - name: 🐳 Build Docker image with tests
        run: docker build --build-arg TEST_GROUP=${{ github.event.inputs.group }} -t qa-tests .

      - name: 📦 Copy zipped Allure report from container
        run: |
          mkdir -p allure-report
          docker run --rm \
            -v ${{ github.workspace }}/allure-report:/out \
            qa-tests sh -c "cp /app/allure-report.zip /out/"

      - name: ⬆️ Upload Allure Report
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: allure-report/allure-report.zip